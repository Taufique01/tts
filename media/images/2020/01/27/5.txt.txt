import cv2 import numpy as np import math image=cv2.imread('apic.jpg',0) image=cv2.resize(image, (300, 300)) img = np.zeros(image.shape,image.dtype); #For log transformation the formula is s=c*log(1+r)  #where c is constant and r is the pixel value of the image c=1 for i in range(image.shape[0]):     for j in range(image.shape[1]):                img[i][j]=np.log(1+image[i][j]) pyp.imshow('log transform',img) cv2.waitKey(0) cv2.destroyAllWindows()